/**
 * Layout de Autenticação
 *
 * Define o layout das páginas de login/cadastro.
 * NÃO deve conter elementos do dashboard (sidebar, mapa, etc).
 *
 * Usado para manter a interface limpa e focada na autenticação.
 */